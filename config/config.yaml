# Project configuration
# TODO: fill in as the pipeline is built.

paths:
  raw_data: data/raw
  processed_data: data/processed
  sources: sources
  results: results

retrieval:
  embedding_model: BAAI/bge-small-en-v1.5
  index_path: data/processed/chunk_index.faiss
  index_ids_path: data/processed/chunk_index_ids.parquet
  top_k: 8
  anthropic_model: claude-sonnet-5
